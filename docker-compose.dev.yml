version: "3"
services:
  node-app: 
    build:
      context: .
      target: development
    volumes:
      - ./src:/app/src:ro ## هنا مهم البايند عشان اراقب التغييرات 
    environment: 
      - NODE_ENV=development
    command: npm run start-dev